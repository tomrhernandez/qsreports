<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user.admin? %>
      <a class="navbar-brand" href="<%= admin_path %>"><i class="glyphicon glyphicon-user"></i> <%= current_user.company_name %></a>
      <% else %>
      <a class="navbar-brand" href="<%= stores_path %>"><i class="glyphicon glyphicon-user"></i> <%= current_user.company_name %></a>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to '<i class="glyphicon glyphicon-log-out"></i> Logout'.html_safe, logout_path, class: "btn btn-danger navbar-btn" %></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</nav>